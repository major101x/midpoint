# secrets: never commit
.secrets/
.env
.env.*
!.env.example
config/extension.env

# build
node_modules/
out/
cache/
broadcast/
*.tar

# tooling
.DS_Store

# vendored Flare scaffold, tracked separately until we fork it
fce-extension-scaffold/
