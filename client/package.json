{
  "name": "@sealed/client",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Client-side order sealing for the Sealed venue. Encrypts orders to a Flare TEE's public key.",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@noble/ciphers": "^1.3.0",
    "@noble/curves": "^1.7.0",
    "@noble/hashes": "^1.6.0",
    "viem": "^2.21.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^2.1.8"
  }
}
